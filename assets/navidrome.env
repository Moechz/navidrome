# ============================================================
# Navidrome 用户配置（由 navidrome.env.example 首装复制而来）
# 修改后执行: systemctl restart navidrome
#
# 说明：监听地址(127.0.0.1)、端口(8453)、子路径(/navidrome)、
# 数据/缓存/音乐目录等安全关键参数已由 systemd 单元写死；
# Navidrome 配置优先级为 环境变量 > 命令行参数，因此本文件里的
# ND_* 变量可覆盖命令行默认值（改端口/目录写这里即可，无需动 unit）。
# 完整选项列表: https://www.navidrome.org/docs/usage/configuration/options/
# ============================================================

# 音乐库目录（可指向 TOS 共享文件夹，如 /mnt/xxx/music，
# 需保证 navidrome 用户可读: chmod -R a+rX <目录>）
#ND_MUSICFOLDER=/var/lib/navidrome/music

# 数据库/缓存目录（一般无需改动）
#ND_DATAFOLDER=/var/lib/navidrome/data
#ND_CACHEFOLDER=/var/lib/navidrome/cache

# 日志级别 error|info|debug|trace
#ND_LOGLEVEL=info

# 禁用匿名洞察数据收集（默认开启，仅收集匿名使用统计）
#ND_ENABLEINSIGHTSCOLLECTOR=false

# 扫描频率（默认 @every 1m；库很大时可放宽，如 @every 15m）
#ND_SCANINTERVAL=@every 15m

# 会话有效期（默认 24h）
#ND_SESSIONTIMEOUT=24h
